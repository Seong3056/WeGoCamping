<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.camping.wego.board.mapper.IBoardMapper">
	<insert id="insert">
		INSERT INTO board
		(id, title, cls, content)
		VALUES
		(#{userId}, #{title}, #{cls}, #{content})
	</insert>
	
	<select id="list" resultType="board">
		SELECT * FROM board
	</select>
	
	<select id="detail" resultType="board">
		SELECT * FROM board WHERE bno = #{bno}
	</select>
	
<<<<<<< HEAD
	<update id="modify">
=======
	<update id="update">
>>>>>>> 57878001a23ad814d3f894fa3ad26501c23f15b3
		UPDATE board SET
		title = #{title}, content= #{content}, hit= #{hit}
		WHERE bno = #{bno}
	</update>
	
</mapper>